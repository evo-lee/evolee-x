/**
 * Shared constants used across components.
 * Centralizes magic numbers that were previously scattered in component files.
 */

/** Max tweet card embeds per chat message */
export const MAX_TWEET_CARDS_PER_MESSAGE = 2;

/** Minimum interval between chat sends (ms) */
export const MIN_SEND_INTERVAL_MS = 3000;

/** Number of skeleton rows in search loading state */
export const SKELETON_ROWS = 5;

/** Desktop breakpoint for article styles (px) */
export const BREAKPOINT_DESKTOP = 960;
